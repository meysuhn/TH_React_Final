import React from 'react';
// import axios from 'axios';


// Set up provider and consumer
const UserAuth = React.createContext({});

export const Provider = UserAuth.Provider;
export const Consumer = UserAuth.Consumer;
